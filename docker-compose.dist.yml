# ❗❗❗ This file is autogenerated! ❗❗❗
# Managed by github.com/MyOnlineStore/repo-templating
# Changing this file will end badly, be wary.

version: '3'
services:
  writer-elector-operator:
    build:
      context: .
      dockerfile: Dockerfile.dev

    volumes:
    - ./src:/srv/app/src:ro
    - ./config:/srv/app/config:ro

networks:

#volumes:
#    app-sync: # OSX users need to enable this section
#      external: true
