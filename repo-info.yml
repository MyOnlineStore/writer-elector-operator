# must be defined in [] yaml structure for bash script to work
templates: [nodejs]

project:
  name: writer-elector-operator

deploy:
  stage:
    enabled: true
    namespace: operator
  prod:
    enabled: true
    namespace: operator

nodejs:
  version: "15"